'use strict';

describe('myApp.rooms module', function() {

  beforeEach(module('myApp.rooms'));

  describe('rooms controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var roomsCtrl = $controller('RoomsCtrl');
      expect(roomsCtrl).toBeDefined();
    }));

  });
});